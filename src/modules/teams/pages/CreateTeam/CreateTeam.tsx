import type { FC } from 'react';
import css from './CreateTeam.module.css';

interface IProps { }

const CreateTeam: FC<IProps> = () => {
  return (
    <div className={css.wrapper}>

    </div>
  );
};

export default CreateTeam;